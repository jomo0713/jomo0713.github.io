# .github/workflows/douban.yml
name: douban
on: 
  schedule:
  - cron: "30 * * * *"

jobs:
  douban:
    name: Douban mark data sync
    runs-on: ubuntu-latest
    steps:
    - name: movie
      uses: jomo/doumark-action@master
      with:
        id: jomo
        type: movie
        format: notion
        dir: xxxx
        notion_token: ${{ secrets.notion_token }}
        
    - name: music
      uses: jomo/doumark-action@master
      with:
        id: jomo
        type: music
        format: notion
        dir: xxxx
        notion_token: ${{ secrets.notion_token }}